<h3 id="page_title">Dagens pasienter</h3>
<span id ="messages">0 uleste meldinger</span><img id="message_icon" src="<%= image_path('message.jpg') %>" />
<% @todays_patients.each do |p| %>
<div class="patients_to_visit" id="<%= p.id %>">
  <img  id="patient_img" src="<%= image_path('gunnar.png') %>">
  <ul>
    <li id="patient_name"><%= "#{p.firstname} #{p.lastname}" %></li>
    <li id="patient_address"><%= "#{p.address}" %><br/>
        <%= "#{p.postnumber} #{p.postplace}" %></li>
  </ul>
  <div class="menu" id="menu<%= p.id %>" style="display: none">
    <table>
      <tr>
        <td><img src="<%= image_path('telephone.jpg') %>" width="30" style="padding-right: 3em" /></td>
        <td><img src="<%= image_path('Map.png') %>" width="30" style="padding-right: 3em"/></td>
        <td><a href="/mobile/patientprofile/<%= p.id %>"><img src="<%= image_path('checklist.jpg') %>" width="30" /></a></td>
      </tr>    
    </table>
  </div>
  
</div>
<% end %>

<% content_for :page_description do %>
  <p>Her får man en oversikt over hvem man skal besøke den dagen. Når man trykker innenfor pasientboksen får man opp tre valg: ringe, kart/veibeskrivelse, oppgaveliste. Her vil man også kunne se om man har fått meldinger fra sentralen.</p>
<% end %>




