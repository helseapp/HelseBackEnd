<%= form_for @task_template, :url => "/task_template/create" do |t| %>
  <%= t.label :name %>:
  <%= t.text_field :name %><br />

  <%= t.label :description %>:<br />
  <%= t.text_area :description %><br />
  
  <%= t.submit %>
  <% end %>
  
