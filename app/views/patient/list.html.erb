<table>
  <tr>
    <th>ID</th>
    <th>Fornavn</th>
    <th>Etternavn</th>
    <th>Adresse</th>
    <th>Poststed</th>

  </tr>



<% @patients.each do |p| %>
<tr>
  <td><%= link_to p.id, :action => "show", :id => p.id %></td>
  <td><%= p.firstname %></td>
  <td><%= p.lastname %></td>
  <td><%= p.address %></td>
  <td><%= p.postplace %></td>
</tr>
<% end %>

</table>

<div id="page_button"><%= link_to "Legg til ny pasient", :action => "new" %></div>